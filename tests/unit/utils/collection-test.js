import collection from 'dummy/utils/collection';
import { module, test } from 'qunit';

module('Unit | Utility | collection');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = collection.create();
  assert.ok(result);
});
