import joinAll from 'dummy/utils/join-all';
import { module, test } from 'qunit';

module('Unit | Utility | join all');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = joinAll();
  assert.ok(result);
});
