import query from 'dummy/utils/query';
import { module, test } from 'qunit';

module('Unit | Utility | query');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = query;
  assert.ok(result.stringify);
  assert.ok(result.parse);
});
