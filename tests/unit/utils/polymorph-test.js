import polymorph from 'dummy/utils/polymorph';
import { module, test } from 'qunit';

module('Unit | Utility | polymorph');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = polymorph();
  assert.ok(result);
});
