import manager from 'dummy/utils/manager';
import { module, test } from 'qunit';

module('Unit | Utility | manager');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = manager();
  assert.ok(result);
});
