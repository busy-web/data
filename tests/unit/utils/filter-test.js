import filter from 'dummy/utils/filter';
import { module, test } from 'qunit';

module('Unit | Utility | filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filter();
  assert.ok(result);
});
