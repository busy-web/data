import filterArray from 'dummy/utils/filter-array';
import { module, test } from 'qunit';

module('Unit | Utility | filter array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterArray;
  assert.ok(result);
});
